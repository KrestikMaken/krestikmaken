# 🧠 OCR Translator

**OCR Translator** — это простое и удобное приложение для Linux, которое позволяет выделить область экрана, распознать текст на изображении (через `tesseract-ocr`), автоматически перевести его (через `translate-shell`) и отобразить результат в аккуратном полупрозрачном окне рядом с выделением.

---

## 🚀 Возможности

✅ Распознавание текста на любом участке экрана (через `tesseract`)  
✅ Автоматический перевод (через `translate-shell`)  
✅ Поддержка русского, английского и других языков  
✅ Окно-перевод поверх экрана (через `yad`)  
✅ Возможность копировать результат в буфер обмена  
✅ Закрытие окна клавишей `ESC`  
✅ Настройки языков перевода (через GUI или аргументы)

---

## 🧩 Зависимости

Для работы требуются следующие пакеты:

```bash
sudo apt install -y slop maim imagemagick tesseract-ocr \
tesseract-ocr-eng tesseract-ocr-rus translate-shell yad \
xdotool xclip x11-utils xcompmgr libnotify-bin
